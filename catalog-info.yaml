# Generated from catalog/fleet.yaml in R055LE/runbook. Do not edit here.
# Change the source and run `scripts/catalog.py apply`; edits made here
# are reported as drift and overwritten.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: runtime-images
  description: "Minimal signed language runtime images with owned rebuild timing, SBOMs, provenance, and vulnerability gates."
  annotations:
    github.com/project-slug: R055LE/runtime-images
  tags:
    - apko
    - containers
    - cosign
    - ghcr
    - library
    - platform
    - python
    - sbom
    - supply-chain
    - wolfi
spec:
  type: library
  lifecycle: production
  owner: R055LE
  domain: platform
  tier: 2
